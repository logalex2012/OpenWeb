services:
  postgres:
    image: postgres:16-alpine
    container_name: openweb-postgres
    restart: unless-stopped
    env_file:
      - .data/database.env
    ports:
      - "5432:5432"
    volumes:
      - ./.data/postgres:/var/lib/postgresql/data
